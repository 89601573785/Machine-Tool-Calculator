# Онлайн-сборщик производственного цеха

**Тема дипломного проекта:** разработка веб-решения создания и конфигурирования производственной линии ООО «ЛЕСКОМ».

Веб-приложение для планирования и визуализации компоновки лесопильной линии: каталог станков, подача бревна, конвейеры на связях, комплексы, расчёт линии и PDF-отчёт. Встроено в сайт ЛЕСКОМ (iframe + API).

## Возможности

- **Каталог** с вкладками: все / станки / подача бревна / конвейеры (справочник) / комплексы; поиск; drag-and-drop
- **Станки** на рабочей области с габаритами из каталога, сеткой и привязкой
- **Подача бревна** — накопитель, кантователь, бревнотаска, поштучная выдача, центрователь (отдельная вкладка, ставятся на поле)
- **Связи** между станками с выбором конвейера или **«Без конвейера»** (пустая связь)
- **Комплексы** — автоматическое развёртывание линии со связями
- Режим **«Связать»** и **Shift + клик** по двум станкам
- Зум, панорамирование, адаптивный шаг сетки
- **Сохранение** в личный кабинет через API (`POST /api/v1/configurator/projects`); открытие по `projectId` из ЛК или админки
- **Расчёт:** производительность линии (min по цепочке), узкое место, энергия, стоимость станков / подачи / конвейеров, установка
- **PDF-отчёт** (компактный, один лист при возможности) с кириллицей
- **postMessage** родителю после сохранения (`leskom:configurator:saved`)

## Стек

| Компонент | Технология |
|-----------|------------|
| Интерфейс | HTML5, CSS3, JavaScript (ES6+) |
| Иконки | Font Awesome 6 |
| PDF | jsPDF, html2canvas |
| Каталог на сайте | API `GET /api/v1/catalog/configurator/equipment` |
| Fallback | SQL.js (`data/factory.db`) / `js/equipment-data.js` |

## Структура

```
├── index.html
├── css/
│   ├── style.css
│   ├── theme.css
│   ├── connections.css
│   └── embed.css
├── js/
│   ├── main.js             # поле, расчёт, PDF, каталог
│   ├── connections.js      # связи, выбор конвейера
│   ├── catalog-meta.js   # catalogType, комплексы, классификация
│   ├── factory-extensions.js
│   ├── integration.js      # embed + API ЛК
│   └── equipment-data.js
├── data/factory.db
├── images/equipment/
├── vendor/
├── scripts/
│   ├── verify-project.mjs
│   ├── verify-integration.mjs
│   └── smoke-logic.mjs
└── docs/
    ├── ROADMAP.md
    └── CONFIGURATOR-INTEGRATION.md
```

## Типы оборудования (`catalogType`)

| Тип | Вкладка | На поле | В расчёте |
|-----|---------|---------|-----------|
| `machine` | Станки | Да | Таблица станков, итог по станкам |
| `log_feed` | Подача бревна | Да | Отдельный блок, если размещено |
| `conveyor` | Конвейеры | Нет (только на связи) | Конвейеры на связях + итог |
| `equipment_complex` | Комплексы | Разворачивается в станки + связи | Как станки |

Классификация: `catalog-meta.js` (имя, slug, legacy id) и API `configurator_catalog.py` на бэкенде.

## Запуск

### Локально (модуль)

```bash
cd Machine-Tool-Calculator
npx serve .
```

Откройте `http://localhost:3000`. При `file://` API и SQLite недоступны — только `equipment-data.js`.

### На сайте leskomphp

Статика копируется в `static/configurator/`:

```bash
node scripts/copy-configurator.mjs
npm run build
```

Конфигуратор: `/configurator/?embed=1&projectId=<uuid>` в iframe из `/cabinet/configurator`.

API: `cd api && uvicorn app.main:app --reload --port 8000`

Подробно: [docs/CONFIGURATOR-INTEGRATION.md](docs/CONFIGURATOR-INTEGRATION.md)

## Верхняя панель

| Элемент | Назначение |
|---------|------------|
| **Название линии** | Заголовок сохранённой конфигурации |
| **Каталог** | Боковая панель оборудования |
| **Очистить** | Убрать всё с поля |
| **Сохранить** | В личный кабинет (на сайте) |
| **Связать** | Режим создания связей |
| **Как пользоваться** | Инструкция (краткая для клиента; расширенная для менеджера и выше) |
| **Клетка** | Шаг сетки |
| **− / + / ⊙** | Масштаб и центр |
| **Рассчитать** | Расчёт, просмотр, PDF |

Меню «Проект» (загрузка черновика, экспорт/импорт JSON) **убрано** — работа только через сохранение на сайте. Раздел для менеджеров в инструкции показывается при роли `manager` и выше или праве `configs.manage` (параметр `staff=1` в URL iframe).

## Управление

| Действие | Как |
|----------|-----|
| Станок / подача бревна | Перетащить из каталога |
| Комплекс | Вкладка «Комплексы» → на поле |
| Связь | «Связать» → источник → приёмник, или **Shift** + два клика |
| Конвейер на связи | В модальном окне; или **Без конвейера** |
| Изменить связь | **ПКМ** по линии |
| Зум | **Ctrl** + колесо или **±** |
| Сдвиг поля | **Alt** / СКМ / ПКМ / ЛКМ по пустому месту |

## Формат проекта (version 2)

```json
{
  "version": 2,
  "title": "Линия СБЦ + Акула",
  "placed": [{ "placementId", "equipmentId", "leskomSlug", "x", "y" }],
  "connections": [{
    "fromId", "toId",
    "conveyorCatalogId": 48,
    "conveyorName": "Рольганги"
  }],
  "view": { "zoom", "panX", "panY" }
}
```

`conveyorCatalogId: null` — пустая связь без конвейера.

## Проверка

```bash
node scripts/verify-project.mjs
node scripts/verify-integration.mjs
node scripts/smoke-logic.mjs
```

## Интеграция с leskomphp

- Деплой: `static/configurator/` → `www/configurator/`
- Vue: `src/views/cabinet/ConfiguratorView.vue`
- Админ: `AdminConfigs.vue` — «Открыть схему» в той же вкладке
- API: `api/app/routers/configurator.py`, `configurator_catalog.py`

Данные каталога — с сайта; цены могут быть «по запросу».

## Лицензия

MIT
